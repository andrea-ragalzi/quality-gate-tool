repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.5.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-added-large-files
      - id: detect-private-key

  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.18.1
    hooks:
      - id: gitleaks

  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.8.0
    hooks:
      - id: ruff
        args: [--fix]
        files: ^backend/
      - id: ruff-format
        files: ^backend/

  - repo: https://github.com/pre-commit/mirrors-prettier
    rev: v3.1.0
    hooks:
      - id: prettier
        types_or: [javascript, jsx, ts, tsx, json, css, scss, markdown]
        exclude: |
            (?x)^(
                backend/.*|
                .*/package-lock.json
            )$

  # Frontend Linting (Disabled until ESLint 9 migration is complete)
  # - repo: local
  #   hooks:
  #     - id: frontend-lint
  #       name: Frontend Lint (Next.js)
  #       entry: bash -c 'cd frontend && ESLINT_USE_FLAT_CONFIG=false npm run lint'
  #       language: system
  #       files: ^frontend/
  #       pass_filenames: false
